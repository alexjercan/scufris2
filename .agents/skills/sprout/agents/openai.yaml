interface:
  display_name: "Sprout"
  short_description: "Manage Scufris worktrees"
  default_prompt: "Use $sprout to manage an isolated worktree for Scufris."
policy:
  allow_implicit_invocation: true
