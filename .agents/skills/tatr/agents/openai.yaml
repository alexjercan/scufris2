interface:
  display_name: "Tatr"
  short_description: "Track Scufris work in Markdown"
  default_prompt: "Use $tatr to inspect and update Scufris tracked tasks."
policy:
  allow_implicit_invocation: true
