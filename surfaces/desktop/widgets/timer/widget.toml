id = "timer"
name = "Timer"
description = "Count down and show the time left, and show that it is up when it is - it does not announce itself. The spawn payload is { seconds: number }, the length. It opens as an exhibit, so it ages out about a minute after the conversation moves past it and the count ends with it: use it for a stretch of the conversation, not for a reminder an hour from now. Its own ticks pause, resume, reset, and add a minute, and hovering it keeps it."
width = 280
height = 185
backend = "timer"
cadence = 200
shared = false
